[package]
name = "libro"
version = "1.0.3"
description = "libro — cryptographic audit chain"
license = "GPL-3.0-only"
language = "cyrius"
cyrius = "4.5.0"

[build]
entry = "src/main.cyr"
output = "libro"
