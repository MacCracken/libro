[package]
name = "libro"
version = "1.1.0"
description = "libro — cryptographic audit chain"
license = "GPL-3.0-only"
language = "cyrius"
cyrius = "5.4.2"

[build]
entry = "src/main.cyr"
output = "libro"
